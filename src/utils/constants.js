// Timeline event icons mapping
export const EVENT_ICONS = {
  achievement: "trophy.png",
  gaming: "controller.png",
  team: "group.png",
};

// Initial number of events to display
export const INITIAL_VISIBLE_COUNT = 4;

// Number of events to load at once when scrolling
export const LOAD_INCREMENT = 1;

// Scroll threshold from bottom to trigger load (in pixels)
export const SCROLL_THRESHOLD = 200;